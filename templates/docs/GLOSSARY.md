# <Project> Glossary

| Term             | Definition |
| ---------------- | ---------- |
| {domain term}    | {plain-language meaning}          |

Keep this file minimal — it exists so that REQUIREMENTS/DESIGN/ARCHITECTURE
don't each re-define shared terms (that is the duplication pattern this
contract exists to prevent).